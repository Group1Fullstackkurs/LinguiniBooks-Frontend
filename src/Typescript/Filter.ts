import { FilterType } from "./EnumFilterType"

const filter = async (filterSetting: FilterType) => {
    switch(filterSetting) {
        case FilterType.az:
            // Uppdatera atom
        break;
    }
        // Ändrar en atom med dom aktiva böckerna
        // beronde på filtertype som den fick
}
export default filter


