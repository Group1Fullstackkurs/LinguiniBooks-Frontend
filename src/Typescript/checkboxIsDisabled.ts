export class checkboxIsDisabled {
    constructor(az:boolean, author: boolean, year: boolean) {
        this.az = az;
        this.author = author;
        this.year = year;
    }
    az: boolean = false;
    author: boolean = false;
    year: boolean = false;
}

export default checkboxIsDisabled